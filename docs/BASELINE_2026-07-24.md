# Baseline freeze - 2026-07-24

This document records what was actually available when the evidence system was initialized and subsequently supplemented on the same date. Missing artifacts are marked missing rather than reconstructed.

## Evidence repository

- Repository: `dollja/pcc-study-evidence`
- Default branch: `main`
- Baseline commit: `30d51976c01c48cd37103495cc5fced01ccc7588`
- Baseline contents: `LICENSE` and a one-line `README.md`
- Baseline status: clean, effectively empty

Initialization and source imports are intentionally isolated in a review branch.

## Prototype repository

Canonical repository:

- Repository: `dollja/interpretive-drift-protoype`
- Default branch: `main`
- Frozen baseline commit: `d6af4b461bd1623fc1021cb1ccc063dbc14498e2`
- Commit date: 2026-07-03
- Latest recorded merge: reviewer walkthrough guide after the Step 1 extractor expansion
- Important prior remediation on main: commit history records removal of `artifact_idd` from detector input and addition of the inspectable coordination-critical-variable extractor.

The canonical source paths are `app.py`, `streamlit_app.py`, `variable_extractor.py`, and `tests/test_variable_extractor.py`. The evidence repository references that immutable commit rather than copying application code. See `docs/PROTOTYPE_REFERENCE.md`.

A local ZIP snapshot was also supplied:

- Filename: `interpretive-drift-prototype-audited-reviewer-walkthrough.zip`
- SHA-256: `e6ec0e16d85704f8fc055227671030b1ef24247399209070094d43fbb8b738d5`
- Size: 487,933 bytes
- Archive date: 2026-06-18
- Local verification: `python -m py_compile *.py` passed; `python -m pytest -q` reported **9 passed**.

The ZIP is **not canonical** because it predates the current prototype main branch. It also contains the known pre-remediation input leak: its `detector.py` constructs match text from `artifact_baseline` and `artifact_idd`. The prototype repository history documents that this was later corrected. The ZIP is retained only as an immutable historical snapshot reference.

## Proposal baseline

An Overleaf/RPI LaTeX source export was supplied after initialization:

- Original filename: `rpi_pcc_LaTeX.zip`
- Import ID: `IMP-0011`
- SHA-256: `52805ac4b32ece8f9bb19dac51e6976e13528fd7dbedc9fe43e45c5d0bcfac61`
- Size: 1,927,622 bytes
- Archive inventory: 35 entries, including 31 files and 4 directories
- Embedded compiled PDF: 47 pages, 666,548 bytes

The unmodified archive is checksum-recorded but is not stored because its front matter contains a personal committee identifier. A repository-safe inventory is stored as `IMP-0012` under `imports/normalized/proposal/2026-07-24/`. It records the path, size, and exact hash of every source-export file without retaining source content or personal committee identifiers. A local privacy-sanitized compile test reused the original figures, completed two `pdflatex` passes, and produced 47 pages. Overleaf remains authoritative for the source files and compiled artifact.

Baseline findings:

- proposal source baseline: **checksum-frozen with repository-safe inventory**
- source title: `Pragmatic Commitment Control: Detecting Latent Interpretive Drift in Collaborative Work`
- canonical author-confirmed title: `Pragmatic Commitment Control: Making Latent Pragmatic Misalignments Observable in Collaborative Work`
- traceability comments: **not yet inserted**
- bibliography mechanism: manual `thebibliography` in `pages/bib.tex`
- proposal citations: 33 unique keys
- manual bibliography records: 38, including 5 currently unused
- local privacy-sanitized compilation status: **two `pdflatex` passes succeeded; 47 pages**

The title mismatch and bibliography conversion are revision tasks, not baseline-import transformations.

## Bibliography baseline

- Complete authoritative Zotero Better BibTeX export: **not yet present**
- `bibliography/references.bib`: retained as an empty Better BibTeX export target
- proposal `sample.bib`: 8 entries; exact standalone/embedded duplicate
- Elicit capability-matrix fragment: 10 entries with opaque keys
- Elicit starter BibTeX snapshot: 35 entries; provisional metadata only
- two originally supplied starter-corpus files were byte-for-byte identical

Import audit findings:

- current proposal citations covered by manual bibliography: 33/33
- current proposal citations covered by `sample.bib`: 8/33
- exact file duplicate: standalone `sample.bib` and proposal-embedded `sample.bib`
- cross-fragment semantic overlaps: `Grounding in communication`, `Dynamics of Public Commitments in Dialogue`, `Common Ground Tracking in Multimodal Dialogue`, and `Safeguarding LLM Agents from Misalignment through Provenance Analysis`

The fragments are preserved without overwrite. Duplicate review, metadata correction, and stable citation-key assignment must occur in Zotero before the authoritative Better BibTeX export replaces the manual proposal bibliography.

## Elicit baseline

Raw inputs are enumerated in `imports/manifest.csv`. Exact duplicates were recorded without storing duplicate bytes. In this text-oriented initialization, binary PDF/XLSX inputs are checksum-recorded and assigned planned immutable paths; the raw BibTeX snapshot and selected normalized CSV derivatives are committed. Binary raw import remains a separate reviewable stage.

Checksum-recorded inputs:

- revised related-work draft PDF - 5 pages
- closest-comparator full-text extraction protocol PDF - 8 pages
- claim-source matrix workbook - `Data!A1:G37`
- combined Tier 1-Tier 2 capability matrix workbook - `Data!A1:R11`
- initial full-text extraction audit workbook - `Data!A1:G13`
- Tier 2 authority/workflow audit workbook - `Data!A1:M7`
- starter literature corpus BibTeX snapshot

Committed normalized derivatives:

- `imports/normalized/elicit/claim_source_matrix.csv`
- `imports/normalized/elicit/initial_full_text_extraction_audit.csv`

The combined capability and Tier 2 workbooks are represented in the canonical claim, source, component-provenance, novelty, and search registers. Their exact binary/raw import and lossless normalized workbook derivatives remain pending a separate binary-capable commit.

## Known limitations at freeze

1. The proposal baseline is frozen, but canonical-title alignment, traceability comments, and staged prose revision remain pending.
2. The complete deduplicated Zotero Better BibTeX export is absent; current `.bib` files are fragments.
3. Current prototype tests were not independently rerun from commit `d6af4b...`; the local ZIP tests apply only to the older snapshot.
4. Elicit metadata is provisional and may contain incomplete author names, venue fields, DOI errors, or abstract-only mechanism judgments.
5. Full-text locators are incomplete in several imported registers.
6. Novelty proposition `NOV-0001` remains a candidate, not a verified priority claim.
7. Binary PDF/XLSX bytes and two lossless normalized workbook derivatives remain pending a separate import commit.
